# Cloudflare Pages configuration
name = "homeboard"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Env vars declared here are exposed both at build time (read by vite.config.ts
# and inlined via `define`) and at runtime to Pages Functions via context.env.
[vars]
# ARTHUR_GCAL_LINK = "https://calendar.google.com/calendar/ical/.../basic.ics"
